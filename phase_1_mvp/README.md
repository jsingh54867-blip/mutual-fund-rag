# Phase 1 - MVP

Implements core building blocks:

- source allowlist and registry
- ingestion pipeline (`4.2 Ingestion Pipeline`)
- scraping service behavior with change detection and run logging

## Run

```bash
python -m ingestion.run_ingestion
```

Run this command from `phase_1_mvp`.